# Pinned, because an oracle that resolves differently next month makes every
# finding it reported unreproducible. Bump deliberately, in its own commit,
# and re-read the corpus report when you do.
rdkit==2025.3.3
openbabel-wheel==3.1.1.21
gemmi==0.7.5
# The third PDBQT dialect (#258). `io/pdbqt.rs` targets AutoDock's own spec
# rather than any toolkit's quirks, and promises to *read* both -- meeko is
# what holds that promise to something. scipy is meeko's own undeclared
# runtime import: `import meeko` fails without it.
meeko==0.8.0
scipy==1.17.1
# The trajectory oracle for XTC/TRR/DCD/NCTRAJ/LAMMPS-trajectory (#340).
# scipy (above) is its hard NCDF dependency -- it tries netCDF4 for faster
# writing and falls back to scipy.io.netcdf when that's absent, which is
# fine for the small fixtures this harness writes.
MDAnalysis==2.10.0
# The geometry oracle for OBJ/PLY (#340). Pure Python + numpy, no system
# libraries, chosen over meshio for its built-in volume/area primitives.
trimesh==5.1.0
