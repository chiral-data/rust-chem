# Generated by Cargo
# will have compiled files and executables
/target/

# Committed: `chem` ships a binary, and `cargo publish --locked` needs it to
# guarantee the published artefact is the one CI verified.
# https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html

# These are backup files generated by rustfmt
**/*.rs.bk


# Added by cargo

/target


# Added by cargo
#
# already existing elements were commented out

#/target
#Cargo.lock

.chiral/

# trunk web build output. The path moved when the crates were reorganised;
# the old `/chem_app/dist/` entry had stopped matching anything.
/crates/chem-app/dist/
dist-e2e/

# The Vercel CLI's project link and the prebuilt output the deploy assembles.
# Listed before anything creates it: #271 was a 4.4 MB wasm bundle committed by
# a `git add` of a directory whose ignore path had gone stale, and the blob is
# still in the object store.
.vercel/

# Python bytecode from the oracle harness. It runs in a container as root,
# so these land in the working tree owned by root and cannot be cleaned up
# by an ordinary `git clean`.
__pycache__/
*.pyc
