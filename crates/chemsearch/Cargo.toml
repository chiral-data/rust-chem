[package]
name = "chemsearch"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true
description = "Fingerprint generation and similarity search across CPU and GPU backends"

[dependencies]
anyhow = { workspace = true }
bitvec = "1.0.1"
log = { workspace = true }

chemcore = { workspace = true }
chemfp = { workspace = true }
chemgpu = { workspace = true }

[dev-dependencies]
pollster = { workspace = true }
# Only the tests build molecules from SMILES; the library takes them as given.
chemio = { workspace = true }
