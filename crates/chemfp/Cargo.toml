[package]
name = "chemfp"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true
description = "Molecular fingerprinting and similarity search for cheminformatics"

[dependencies]
bitvec = "1.0.1"
fxhash = "0.2.1"
rayon = { workspace = true }
thiserror = { workspace = true }

chemio = { workspace = true }
chemcore = { workspace = true }

[dev-dependencies]
# Tests only. The library is the CPU implementation; chemgpu's kernels are
# validated against it in tests/gpu_cpu_parity.rs. Keeping this out of
# [dependencies] is what stops wgpu reaching every consumer.
chemgpu = { workspace = true }
