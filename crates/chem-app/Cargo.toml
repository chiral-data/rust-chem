[package]
name = "chem-app"
version.workspace = true
edition = "2024"
# A GUI binary, not a library. Excluded from the registry explicitly rather
# than by omission from a publish command.
publish = false

[dependencies]
chem = { workspace = true, features = ["gpu"] }
eframe = { workspace = true }
egui = { workspace = true }
egui_extras = { workspace = true }
rfd = { workspace = true }
pollster = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
log = { workspace = true }
web-time = "1.1.0"


[dependencies.bitvec]
version = "1.0"
features = ["std"]

[target.'cfg(windows)'.dependencies]
winit = { version = "0.30", features = ["rwh_06"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
env_logger = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3.77"
wasm-bindgen = "0.2.100"
wasm-bindgen-futures = "0.4.0"
console_error_panic_hook = "0.1"

[target.'cfg(target_arch = "wasm32")'.dependencies.web-sys]
version = "0.3.77"
features = [
    "Blob",
    "BlobPropertyBag",
    "Document",
    "Element",
    "HtmlAnchorElement",
    "HtmlCanvasElement",
    "Url",
    "Window",
]

[dev-dependencies]
# Drives a view headlessly and queries what it rendered, through the
# accessibility tree egui already builds. Its own deps are egui 0.33.3,
# eframe 0.33.3 and wgpu 27.0.1 -- exactly the versions here -- and it leaves
# eframe and wgpu optional, so this needs no GPU and no window (#303).
egui_kittest = "0.33.3"
